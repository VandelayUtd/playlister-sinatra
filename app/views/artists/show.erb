
    <h3>Artist: <%=@slug.name%></h3> 
    <h4>Songs:</h4>
    <%@slug.songs.each do |song|%>
        <a href="/songs/<%=song.slug%>"><%=song.name%></a>
    <%end%>
    <h4>Genres:</h4>
    <%@slug.genres.each do |genre|%> 
        <a href="/genres/<%=genre.slug%>"><%=genre.name%></a>
    <%end%>